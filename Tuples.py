from tokenize import cookie_re


coordinates = (4, 5)                                    # Once a tuple is created, it can't be modified
# coordinates[1] = 7                  NOT POSSIBLE
print(coordinates[1])                                   # Elements inside a tuple can also be accessed through indexing
